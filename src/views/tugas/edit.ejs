<%- include('../partials/header') %>

<div class="min-h-screen bg-gray-50 py-8">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="bg-white rounded-lg shadow-sm p-6">
      <div class="pb-5 border-b border-gray-200">
        <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">
          Edit Tugas
        </h2>
      </div>

      <form action="/tugas/edit/<%= tugas.id %>" method="POST" class="mt-6 space-y-6">
        <div class="grid grid-cols-1 gap-y-6 gap-x-4 sm:grid-cols-6">
          <div class="sm:col-span-6">
            <label for="mata_kuliah" class="block text-sm font-medium text-gray-700">Mata Kuliah</label>
            <input type="text" name="mata_kuliah" id="mata_kuliah" value="<%= tugas.mata_kuliah %>" required class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
          </div>

          <div class="sm:col-span-6">
            <label for="deskripsi" class="block text-sm font-medium text-gray-700">Deskripsi Tugas</label>
            <textarea id="deskripsi" name="deskripsi" rows="3" required class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm"><%= tugas.deskripsi %></textarea>
            <p class="mt-2 text-sm text-gray-500">Jelaskan detail tugas yang perlu dikerjakan</p>
          </div>

          <div class="sm:col-span-3">
            <label for="deadline_tanggal" class="block text-sm font-medium text-gray-700">Tanggal Deadline</label>
            <input type="date" name="deadline_tanggal" id="deadline_tanggal" value="<%= tugas.deadline_tanggal %>" required class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
          </div>

          <div class="sm:col-span-3">
            <label for="deadline_waktu" class="block text-sm font-medium text-gray-700">Waktu Deadline</label>
            <input type="time" name="deadline_waktu" id="deadline_waktu" value="<%= tugas.deadline_waktu %>" required class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
          </div>

          <div class="sm:col-span-6">
            <label for="tempat_pengumpulan" class="block text-sm font-medium text-gray-700">Tempat Pengumpulan</label>
            <input type="text" name="tempat_pengumpulan" id="tempat_pengumpulan" value="<%= tugas.tempat_pengumpulan %>" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" placeholder="Contoh: Google Classroom, E-learning, dsb">
          </div>

          <div class="sm:col-span-6">
            <label class="block text-sm font-medium text-gray-700">Last Notified</label>
            <div class="flex flex-col space-y-2">
              <label class="inline-flex items-center">
                <input type="radio" name="last_notified_action" value="keep" checked>
                <span class="ml-2">Biarkan (tidak mengubah last_notified)</span>
              </label>
              <label class="inline-flex items-center">
                <input type="radio" name="last_notified_action" value="null">
                <span class="ml-2">Kosongkan (hapus/NULL-kan last_notified)</span>
              </label>
            </div>
            <div class="mt-3 p-4 bg-red-50 border-l-4 border-red-500 rounded-md">
              <div class="flex">
                <div class="flex-shrink-0">
                  <svg class="h-5 w-5 text-red-500" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
                  </svg>
                </div>
                <div class="ml-3">
                  <h3 class="text-sm font-medium text-red-800">Peringatan Akses Terbatas</h3>
                  <div class="mt-2 text-sm text-red-700">
                    <p>Mohon untuk tidak melakukan perubahan pada kolom last_notified apabila Anda bukan administrator resmi dari sistem fs-server.icu.</p>
                    <p class="mt-1 text-sm text-red-600">Perubahan tanpa otorisasi dapat mengganggu fungsi notifikasi sistem dan berpotensi menyebabkan ketidaksesuaian data.</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="flex justify-end space-x-3 pt-5">
          <a href="/tugas" class="inline-flex justify-center py-2 px-4 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
            Batal
          </a>
          <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
            Simpan Perubahan
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const radios = document.getElementsByName('last_notified_action');
    const input = document.getElementById('last_notified');
    radios.forEach(radio => {
      radio.addEventListener('change', function() {
        if (this.value === 'set') {
          input.disabled = false;
        } else {
          input.disabled = true;
        }
      });
    });
  });
</script>

<%- include('../partials/footer') %> 